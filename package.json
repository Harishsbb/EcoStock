{
  "name": "smartstock-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm --prefix client run build && node -e \"fs.cpSync('client/dist', 'dist', {recursive: true})\"",
    "vercel-build": "npm --prefix client run build && node -e \"fs.cpSync('client/dist', 'dist', {recursive: true})\""
  }
}
